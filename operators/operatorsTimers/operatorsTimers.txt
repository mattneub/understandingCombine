#title "Timers"
#subtitle "playing with time"

# <%= title %>

Timers
: By *timers* I mean those operators that play with the timing of the values arriving from upstream.

The timer operator _par excellence_ is `.delay`, which inserts a pause between the time a value arrives from upstream and the time it passes downstream.

